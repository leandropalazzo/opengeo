[workspace]
resolver = "2"
members = [
    "apps/api",
    "apps/worker",
    "apps/cli",
    "apps/mcp",
    "crates/core",
    "crates/providers",
    "crates/extractors",
    "crates/storage",
    "crates/analytics",
    "crates/crawler-ingest",
    "crates/scheduler",
    "crates/plugin-host",
    "crates/plugin-manifest",
    "crates/wire-schema",
    "crates/benchmark",
    "crates/recommendations",
    "crates/audit",
    "crates/comms",
    "crates/authn",
    "crates/authz",
    "crates/billing",
]

[workspace.package]
edition = "2021"
rust-version = "1.95"
license = "MIT"

[workspace.dependencies]
tokio = { version = "1", features = ["full"] }
anyhow = "1"
thiserror = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "json"] }
tracing-bunyan-formatter = "0.3"
tokio-test = "0.4"
sqlx = { version = "0.8", features = ["runtime-tokio", "tls-rustls", "macros", "migrate", "postgres", "uuid", "chrono"] }
uuid = { version = "1", features = ["v4", "serde"] }
wiremock = "0.6"
insta = { version = "1", features = ["yaml", "json"] }
proptest = "1"
tracing-test = "0.2"
ulid = { version = "1", features = ["serde"] }
secrecy = { version = "0.8", features = ["serde"] }
utoipa = { version = "5", features = ["chrono", "preserve_order"] }
schemars = "0.8"
chrono = { version = "0.4", features = ["serde"] }
chrono-tz = "0.10"
serde_yaml = "0.9"
sha2 = "0.10"
hmac = "0.12"
subtle = "2.5"
ed25519-dalek = "2"
hex = "0.4"
clap = { version = "4", features = ["derive", "wrap_help", "env"] }
dialoguer = { version = "0.11", default-features = false }
tempfile = "3"
assert_cmd = "2"
predicates = "3"
is-terminal = "0.4"
keyring = { version = "3", default-features = false, features = ["sync-secret-service", "apple-native", "windows-native"] }
age = { version = "0.10", default-features = false, features = ["armor"] }
rpassword = "7"

# async-stripe-shared is a massive generated file that OOM-kills the Depot builder.
# opt-level=1 is not enough; opt-level=0 + codegen-units=16 keeps per-unit allocation low.
# Runtime perf is negligible — pure HTTP plumbing/deserialization.
[profile.release.package.async-stripe-shared]
opt-level = 0
codegen-units = 16
